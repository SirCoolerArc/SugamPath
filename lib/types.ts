// All shared TypeScript types live in this file. Do not scatter type definitions
// across the codebase. Populated incrementally as each Stage 0 module lands.

export interface Placeholder {
  readonly _placeholder: true;
}
