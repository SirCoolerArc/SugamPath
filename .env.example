GEMINI_API_KEY=
# Google Drive API key (restricted to Drive API, read-only on public folders).
# Used by scripts/sync_isl_dictionary.ts to enumerate the ISLRTC dictionary
# folders published on data.gov.in. Also embedded in the videoUrls in
# data/isl_dictionary.json so the chip's <video> element can stream directly.
GOOGLE_DRIVE_API_KEY=
